.env
*.env
.env.*
*.key
*.pem
*.secret
n8n_storage/
postgres_storage/
ollama_storage/
qdrant_storage/
__pycache__/
*.pyc
.DS_Store
/tmp/
mobile/node_modules/
mobile/.expo/
mobile/dist/

# Binaries / packages (don't belong in git)
*.deb
*.iso

# Scratch / working notes (not for public repo)
dashboard-adding-pve.txt
dns-fixup.txt
nola-troubleshooting.txt
integrations/hola-nola-creation.txt

# Internal planning docs (not for public repo)
ORIGINAL_JEEVES_DUMP.md
README_FIRST.md
PROJECT_README.md
SETUP_INSTRUCTIONS.md
jeeves-main-workflow.json

# Build transcript (contains secrets — never publish)
nola-creation.txt

# Local Claude Code settings
.claude/
**/.claude/
*.local.json

# Workflow backups — contain live credentials (Discord bot tokens, API keys)
backups/

# Syncthing conflict copies. This tree is replicated; an Aug-2026 conflict
# silently dropped work, so keep the copies on disk but never commit them.
*.sync-conflict-*

# Local editor/deploy backups
*.bak-*
*.bak

# Stray Claude Code session transcripts
temperature.txt
