services:
  nola-dashboard:
    build: .
    container_name: nola-dashboard
    restart: unless-stopped
    env_file: .env
    networks:
      18_Net:
        ipv4_address: 10.0.6.43

networks:
  18_Net:
    external: true
